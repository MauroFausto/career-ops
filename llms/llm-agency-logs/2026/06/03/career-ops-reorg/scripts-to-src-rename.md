## 2026-06-03 - sessão career-ops
- Renomeado `scripts/` → `src/`; refs atualizadas em package.json, modes, docs, CI, batch-runner, test-all;
- Análise de adapter LaTeX (tex-docctor via MCP) + gap analysis + typoruler explicado.

--- TimeLine ---
- `mv scripts src` + sed bulk `scripts/` → `src/` (~42 arquivos)
- Fix manual: `test-all.mjs` findMjsFiles(ROOT, 'src')
- README structure, DATA_CONTRACT, labeler.yml, sequence diagram
